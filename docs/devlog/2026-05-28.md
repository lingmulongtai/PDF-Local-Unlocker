# Devlog - 2026-05-28

## 今日の目的

- PDF Local Unlocker の新規開発を開始する
- Phase 0 と Phase 1 の最初のUIモックまで作る
- READMEに毎回進捗を残す運用を始める

## 実装・修正したこと

- Vite + React + TypeScript + Tailwind CSS の構成を追加
- Reactアプリの初期画面を実装
- ドラッグ&ドロップ、複数PDFキュー、共通パスワード入力、ファイル別パスワード入力を追加
- 状態バッジ、進捗バー、再試行、個別ダウンロード、ZIPダウンロードのUIを追加
- qpdf-wasm Workerの型とプレースホルダーを追加
- パスワードを永続化しない方針をUIとREADMEに明記
- GitHub Actionsで `dist/` をGitHub Pagesへ自動デプロイするワークフローを追加
- Pages artifactに `.nojekyll` が含まれるように `public/.nojekyll` を追加

## 変更したファイル

- README.md
- TODO.md
- .github/workflows/deploy.yml
- public/.nojekyll
- package.json
- package-lock.json
- vite.config.ts
- tsconfig.json
- tsconfig.app.json
- tsconfig.node.json
- index.html
- public/favicon.svg
- src/main.tsx
- src/App.tsx
- src/types.ts
- src/components/*
- src/lib/*
- src/workers/qpdf.worker.ts
- src/styles/globals.css
- docs/decisions.md
- docs/devlog/2026-05-28.md
- docs/devlog/2026-05-28-home.png

## 確認したこと

- `npm install` が成功した
- `npm run build` が成功した
- ローカル開発サーバー `http://127.0.0.1:5173/` で画面表示を確認した
- 初期画面のスクリーンショットを保存した
- ブラウザのエラーログにエラーや警告がないことを確認した
- `src` 内にパスワードを `console.log` する実装がないことを確認した
- `dist/.nojekyll` が生成されることを確認した

## うまくいかなかったこと

- この環境では通常の `node` / `npm` がWindowsApps側を参照して `Access is denied` になった
- 一時展開したnpm CLIとCodex同梱Nodeを使って依存インストールとビルドを実行した
- qpdf-wasmの実解除処理はまだ接続していない

## 判断したこと

- 初回はqpdf実処理まで入れず、ビルド可能なUIとWorker接続点を優先した
- Viteの `base` はGitHub Pagesで扱いやすいように `./` にした
- Tailwind CSSは公式のViteプラグイン構成にした
- GitHub PagesデプロイはGitHub公式のPages Actionsを使うことにした

## README更新

- 更新した
- 理由: 初期セットアップ、現在の進捗、既知の制限、次の予定を記録するため

## 次にやること

- qpdf-wasmをWorker内で初期化する
- 1ファイル解除の実処理を実装する
- wrong password のエラー分類を実装する
- スマホ表示を追加確認する
- GitHub Actions上でPagesデプロイ成功を確認する
